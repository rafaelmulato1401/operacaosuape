{
  "name": "inpasa-gestao-estoque",
  "version": "1.0.0",
  "private": true,
  "description": "INPASA - Sistema de Gestao de Estoque",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && vercel --prod"
  },
  "dependencies": {
    "firebase": "^10.12.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
